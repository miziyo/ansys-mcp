# Changelog

## 0.13.0

- Publish only the project-owned core thermal runner, CLI, MCP facade, schemas, generated test
  geometry, and tests.
- Remove every official-example catalog, adapter, runner, fixture, inventory, qualification record,
  and historical gallery document from the public distribution.
- Remove the tutorial MCP tools; the public STDIO surface now contains ten bounded thermal and job
  operations.
- Remove qualification-only product clients, scripts, dependency groups, and generated solver
  assets from the public package.
- Add a fail-closed public-content rule that rejects tutorial paths and common solver/project output
  formats.
- Retain dynamic Ansys installation discovery and the fixed Prime → MAPDL → DPF thermal route.
